# Environment Variables Configuration

# Database
DATABASE_URL=postgresql://user:password@localhost:5432/your_coach

# Session Secret (generate a random string)
SESSION_SECRET=your_random_secret_key_here_change_in_production

# OpenAI API Configuration
AI_INTEGRATIONS_OPENAI_API_KEY=your_openai_api_key_here
AI_INTEGRATIONS_OPENAI_BASE_URL=https://api.openai.com/v1

# Optional: Specific model to use (default: gpt-4o-mini)
OPENAI_MODEL=gpt-4o-mini

# Server Port (default: 5000)
PORT=5000

# Node Environment (development | production)
NODE_ENV=development
